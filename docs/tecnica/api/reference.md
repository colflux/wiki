# Referencia de API

> Pendiente de contenido. Si Colflux expone un spec OpenAPI, se recomienda generar esta página automáticamente
> con un plugin como `mkdocs-swagger-ui-tag` o `mkdocs-render-swagger-plugin` en lugar de mantenerla a mano.

## Endpoints

### `GET /ejemplo`

Descripción, parámetros, respuesta de ejemplo.
